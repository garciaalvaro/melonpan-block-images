const { __ } = wp.i18n;

const plugin_namespace = "melonpan-block";
const pr = "mbi";
const plugin_title = __("Melonpan Block - Images");
const plugin_description = __(
	"Add a gallery of images in a simple but clean layout."
);

export { plugin_namespace, pr, plugin_title, plugin_description };
